#!/bin/bash

set -e

BUILD_DIR=$1
CACHE_DIR=$2

indent() {
  sed -u 's/^/       /'
}

echo "-----> Configure /app directories & symlink /app to web root " + $BUILD_DIR

mkdir -p /var/www
ln -sf /var/www/app.amjcampbell.com /app
ln -sf /var/www/amj /app
